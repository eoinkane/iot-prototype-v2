{
  "name": "lambda_app",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "run-local-lambda": "node test/local/index.js",
    "build": "mkdir lambda-app-build && cp -r src/** lambda-app-build && cp package* lambda-app-build && cd lambda-app-build && npm i --only=prod && zip -r lambda-app-build.zip . && cd ../",
    "clean": "rm -rf lambda-app-build || true && rm src/**/*.js || true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-lambda-router": "^0.11.0",
    "dotenv": "^10.0.0",
    "mysql2": "^2.3.0"
  },
  "devDependencies": {
    "typescript": "^4.4.4"
  }
}
