{
  "name": "lambda_app",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "compile:src": "tsc src/**.ts",
    "compile:test": "tsc test/**/*.ts",
    "run-local-lambda": "node test/local/index.js",
    "build": "mkdir -p lambda-app-build && cp -r src/** lambda-app-build && cp package* lambda-app-build && cd lambda-app-build && npm i --only=prod && zip -r lambda-app-build.zip . && cd ../",
    "clean": "rm -rf lambda-app-build src/**/*.js || true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-lambda-router": "^0.11.0",
    "dotenv": "^10.0.0",
    "mysql2": "^2.3.0"
  },
  "devDependencies": {
    "ts-node": "^10.4.0",
    "typescript": "^4.4.4"
  }
}
