{
  "name": "lambda_app",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "run-local-lambda": "node test/local/index.js",
    "build": "mkdir lambda-app-build && cp -r src lambda-app-build && cp -r node_modules lambda-app-build && cd lambda-app-build && zip -r lambda-app-build.zip . && cd ../",
    "clean": "rm -rf lambda-app-build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-lambda-router": "^0.11.0",
    "dotenv": "^10.0.0",
    "mysql2": "^2.3.0"
  }
}
